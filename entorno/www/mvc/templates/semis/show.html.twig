{% extends 'base.html.twig' %}

{% block title %}Semis{% endblock %}

{% block body %}
    <h1>Semis</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ semi.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_semis_index') }}">back to list</a>

    <a href="{{ path('app_semis_edit', {'id': semi.id}) }}">edit</a>

    {{ include('semis/_delete_form.html.twig') }}
{% endblock %}
