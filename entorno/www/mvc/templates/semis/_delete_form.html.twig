<form method="post" action="{{ path('app_semis_delete', {'id': semi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ semi.id) }}">
    <button class="btn">Delete</button>
</form>
