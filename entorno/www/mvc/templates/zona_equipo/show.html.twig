{% extends 'base.html.twig' %}

{% block title %}ZonaEquipo{% endblock %}

{% block body %}
    <h1>ZonaEquipo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ zona_equipo.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_zona_equipo_index') }}">back to list</a>

    <a href="{{ path('app_zona_equipo_edit', {'id': zona_equipo.id}) }}">edit</a>

    {{ include('zona_equipo/_delete_form.html.twig') }}
{% endblock %}
