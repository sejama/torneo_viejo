<form method="post" action="{{ path('app_cuartos_delete', {'id': cuarto.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cuarto.id) }}">
    <button class="btn">Delete</button>
</form>
