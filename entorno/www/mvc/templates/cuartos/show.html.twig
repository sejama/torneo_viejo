{% extends 'base.html.twig' %}

{% block title %}Cuartos{% endblock %}

{% block body %}
    <h1>Cuartos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cuarto.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cuartos_index') }}">back to list</a>

    <a href="{{ path('app_cuartos_edit', {'id': cuarto.id}) }}">edit</a>

    {{ include('cuartos/_delete_form.html.twig') }}
{% endblock %}
