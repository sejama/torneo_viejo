{% extends 'base.html.twig' %}

{% block title %}Fin{% endblock %}

{% block body %}
    <h1>Fin</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fin.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_fin_index') }}">back to list</a>

    <a href="{{ path('app_fin_edit', {'id': fin.id}) }}">edit</a>

    {{ include('fin/_delete_form.html.twig') }}
{% endblock %}
