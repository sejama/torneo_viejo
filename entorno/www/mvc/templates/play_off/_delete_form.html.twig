<form method="post" action="{{ path('app_play_off_delete', {'id': play_off.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ play_off.id) }}">
    <button class="btn">Delete</button>
</form>
