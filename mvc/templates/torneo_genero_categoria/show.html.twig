{% extends 'base.html.twig' %}

{% block title %}Configurar Genero Categoria{% endblock %}

{% block body %}
    <h1>Configurar Torneo Genero Categoria</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ torneo_genero_categorium.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_torneo_genero_categoria_index') }}">Volver</a>

    <a href="{{ path('app_torneo_genero_categoria_edit', {'id': torneo_genero_categorium.id}) }}">Modificar</a>

    {{ include('torneo_genero_categoria/_delete_form.html.twig') }}
{% endblock %}
