{% extends 'base.html.twig' %}

{% block title %}Club{% endblock %}

{% block body %}
    <h1>Club</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ club.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ club.nombre }}</td>
            </tr>
            <tr>
                <th>Dirección</th>
                <td>{{ club.direccion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_club_index') }}">Volver</a>

    <a href="{{ path('app_club_edit', {'id': club.id}) }}">Editar</a>

    {{ include('club/_delete_form.html.twig') }}
{% endblock %}
