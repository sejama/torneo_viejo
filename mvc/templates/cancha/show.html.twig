{% extends 'base.html.twig' %}

{% block title %}Cancha{% endblock %}

{% block body %}
    <h1>Cancha</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cancha.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ cancha.nombre }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ cancha.createdAt ? cancha.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ cancha.updatedAt ? cancha.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cancha_index') }}">back to list</a>

    <a href="{{ path('app_cancha_edit', {'id': cancha.id}) }}">edit</a>

    {{ include('cancha/_delete_form.html.twig') }}
{% endblock %}
