{% extends 'base.html.twig' %}

{% block title %}Cancha{% endblock %}

{% block body %}
    <h1>Cancha</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cancha.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ cancha.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cancha_index') }}">Volver</a>

    <a href="{{ path('app_cancha_edit', {'id': cancha.id}) }}">Modificar</a>

    {{ include('cancha/_delete_form.html.twig') }}
{% endblock %}
