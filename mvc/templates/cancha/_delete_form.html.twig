<form method="post" action="{{ path('app_cancha_delete', {'id': cancha.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cancha.id) }}">
    <button class="btn">Delete</button>
</form>
